[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/tournament-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
restartPolicyType = "on_failure"
